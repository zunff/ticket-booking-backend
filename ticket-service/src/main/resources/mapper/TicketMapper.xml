<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ticketbooking.ticket.mapper.TicketMapper">

    <select id="findAvailableTickets" resultType="com.ticketbooking.ticket.entity.Ticket">
        SELECT * FROM tickets
        WHERE available_stock > 0
          AND start_time &lt;= NOW()
          AND end_time &gt;= NOW()
    </select>

    <select id="findByIdForUpdate" resultType="com.ticketbooking.ticket.entity.Ticket">
        SELECT * FROM tickets WHERE id = #{id} FOR UPDATE
    </select>

    <update id="decrementStock">
        UPDATE tickets
        SET available_stock = available_stock - #{quantity},
            update_time = NOW()
        WHERE id = #{id}
          AND available_stock &gt;= #{quantity}
    </update>

    <update id="incrementStock">
        UPDATE tickets
        SET available_stock = available_stock + #{quantity},
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
