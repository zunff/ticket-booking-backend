apiVersion: v1
kind: Service
metadata:
  name: ticket-order-service
  namespace: ticket-booking
  labels:
    app: ticket-order-service
spec:
  ports:
  - port: 8082
    name: http
    targetPort: 8082
  selector:
    app: ticket-order-service
