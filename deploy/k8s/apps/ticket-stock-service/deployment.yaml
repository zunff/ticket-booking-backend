apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-stock-service
  namespace: ticket-booking
  labels:
    app: ticket-stock-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ticket-stock-service
  template:
    metadata:
      labels:
        app: ticket-stock-service
    spec:
      initContainers:
      - name: wait-for-nacos
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z nacos-headless 8848; do echo waiting for nacos; sleep 2; done']
      - name: wait-for-kafka
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z kafka-headless 9092; do echo waiting for kafka; sleep 2; done']
      containers:
      - name: ticket-stock-service
        image: ticket-booking/ticket-stock-service:1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8083
          name: http
        envFrom:
        - configMapRef:
            name: app-config
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: sentinel.transport.port
          value: "8721"
        - name: JAVA_OPTS
          value: "-Xmx1024m -Xms512m -XX:+UseG1GC"
        resources:
          requests:
            cpu: "500m"
            memory: "768Mi"
          limits:
            cpu: "1500m"
            memory: "1280Mi"
        livenessProbe:
          httpGet:
            path: /stock/actuator/health
            port: 8083
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /stock/actuator/health
            port: 8083
          initialDelaySeconds: 60
          periodSeconds: 5
          timeoutSeconds: 3
