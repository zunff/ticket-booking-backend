server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: user-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}
  threads:
    virtual:
      enabled: true
