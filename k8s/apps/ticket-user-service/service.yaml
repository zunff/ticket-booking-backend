apiVersion: v1
kind: Service
metadata:
  name: ticket-user-service
  namespace: ticket-booking
  labels:
    app: ticket-user-service
spec:
  ports:
  - port: 8081
    name: http
    targetPort: 8081
  selector:
    app: ticket-user-service
