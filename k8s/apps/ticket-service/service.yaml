apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: ticket-booking
  labels:
    app: ticket-service
spec:
  ports:
  - port: 8080
    name: http
    targetPort: 8080
  selector:
    app: ticket-service
